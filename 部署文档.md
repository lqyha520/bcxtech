# 布草芯科技官网 - 部署文档

本文档详细说明如何部署布草芯科技官网项目到生产环境。

## 📋 目录

- [部署前准备](#部署前准备)
- [环境变量配置](#环境变量配置)
- [构建项目](#构建项目)
- [部署方式](#部署方式)
  - [Vercel（推荐）](#vercel推荐)
  - [自己的服务器](#自己的服务器)
  - [Docker 部署](#docker-部署)
  - [其他平台](#其他平台)
- [部署后验证](#部署后验证)
- [性能优化](#性能优化)
- [常见问题](#常见问题)
- [监控和维护](#监控和维护)

---

## 🚀 部署前准备

### 1. 检查项目依赖

确保项目依赖已正确安装：

```bash
npm install
```

### 2. 本地测试

在部署前，先在本地测试构建：

```bash
# 构建生产版本
npm run build

# 启动生产服务器测试
npm run start
```

访问 `http://localhost:3000` 确认网站正常运行。

### 3. 准备图片资源

确保以下图片文件已添加到 `public` 目录：

- ✅ `favicon.svg` - 网站图标（已创建）
- ✅ `icon.svg` - 通用图标（已创建）
- ✅ `og-image.svg` - OpenGraph 图片（已创建）
- ✅ `logo.svg` - Logo（已创建）
- ⚠️ `favicon.ico` - 传统 favicon（可选，建议生成）
- ⚠️ `apple-touch-icon.png` - Apple 设备图标（可选，建议生成）
- ⚠️ `og-image.jpg` - OpenGraph 图片 JPG 版本（可选，建议生成）

> 💡 查看 `scripts/generate-favicons.md` 了解如何生成这些图片。

### 4. 代码检查

运行代码检查确保没有错误：

```bash
npm run lint
```

---

## 🔐 环境变量配置

### 必需的环境变量

创建 `.env.local` 文件（本地开发）或在部署平台配置环境变量：

```env
# 网站 URL（必需）
NEXT_PUBLIC_SITE_URL=https://your-domain.com

# 其他可选环境变量
# NODE_ENV=production
```

### 环境变量说明

| 变量名 | 说明 | 是否必需 | 默认值 |
|--------|------|----------|--------|
| `NEXT_PUBLIC_SITE_URL` | 网站的完整 URL，用于 SEO 和 OpenGraph | ✅ 是 | `https://bucaoxin.com` |
| `NODE_ENV` | 环境模式（production/development） | ❌ 否 | `production`（构建时自动设置） |

### 在各平台配置环境变量

#### Vercel
1. 进入项目设置
2. 选择 "Environment Variables"
3. 添加变量并选择环境（Production, Preview, Development）
4. 保存后重新部署

#### 自己的服务器
在服务器上创建 `.env.production` 文件：

```bash
# 在服务器上
echo "NEXT_PUBLIC_SITE_URL=https://your-domain.com" > .env.production
```

#### Docker
在 `docker-compose.yml` 或 Dockerfile 中配置：

```yaml
environment:
  - NEXT_PUBLIC_SITE_URL=https://your-domain.com
```

#### Cloudflare Pages
1. 进入项目设置
2. 选择 "Settings" → "Environment variables"
3. 添加变量并选择环境（Production, Preview, Browser preview）
4. 保存后重新部署

---

## 🏗️ 构建项目

### 本地构建

```bash
# 安装依赖
npm install

# 构建生产版本
npm run build
```

构建成功后，会生成 `.next` 目录，包含优化后的生产代码。

### 构建输出

构建完成后，检查以下内容：

- ✅ `.next` 目录已生成
- ✅ 没有构建错误或警告
- ✅ 静态资源已优化
- ✅ 图片已优化（WebP/AVIF）

### 构建优化

项目已配置以下优化：

- ✅ 代码压缩（SWC Minify）
- ✅ 生产环境移除 console
- ✅ 图片优化（WebP/AVIF）
- ✅ 静态资源缓存策略
- ✅ 字体优化（preload）

---

## 🚀 部署方式

### Vercel（推荐，但国内访问可能受限）

Vercel 是 Next.js 官方推荐的部署平台，提供最佳的性能和体验。

⚠️ **重要提示 - 中国大陆访问限制**：
- Vercel 的 `.vercel.app` 域名在中国大陆可能无法访问或访问速度较慢
- 即使使用自定义域名，由于网络环境限制，访问可能仍不稳定
- **如果主要面向中国用户，强烈建议使用国内云服务商（阿里云、腾讯云、华为云等）**

💡 **改善 Vercel 国内访问的方法**：
1. 使用自定义域名 + 国内 CDN（如阿里云 CDN、腾讯云 CDN）
2. 配置 DNS 解析到国内 CDN 节点
3. 使用国内服务器作为反向代理

#### 方式一：通过 Vercel Dashboard

1. **登录 Vercel**
   - 访问 [vercel.com](https://vercel.com)
   - 使用 GitHub/GitLab/Bitbucket 账号登录

2. **导入项目**
   - 点击 "Add New Project"
   - 选择 GitHub 仓库
   - 点击 "Import"

3. **配置项目**
   - **Framework Preset**: Next.js（自动检测）
   - **Root Directory**: `./`（默认）
   - **Build Command**: `npm run build`（默认）
   - **Output Directory**: `.next`（默认）
   - **Install Command**: `npm install`（默认）

4. **环境变量**
   - 添加 `NEXT_PUBLIC_SITE_URL` 环境变量
   - 值为你的域名，如：`https://your-domain.com`

5. **部署**
   - 点击 "Deploy"
   - 等待构建完成（通常 1-3 分钟）

6. **自定义域名**
   - 在项目设置中添加自定义域名
   - 配置 DNS 记录
   - 等待 SSL 证书自动配置

#### 方式二：通过 Vercel CLI

```bash
# 安装 Vercel CLI
npm i -g vercel

# 登录
vercel login

# 部署
vercel

# 生产环境部署
vercel --prod
```

#### Vercel 优势

- ✅ 自动 HTTPS
- ✅ 全球 CDN
- ✅ 自动部署（Git 推送触发）
- ✅ 预览部署（每个 PR 自动生成预览链接）
- ✅ 性能监控
- ✅ 免费额度充足

---

### 自己的服务器

#### 系统要求

- Node.js 18.17 或更高版本
- npm 或 yarn
- 至少 1GB RAM
- 至少 10GB 磁盘空间

#### 部署步骤

##### 1. 安装 Node.js

```bash
# Ubuntu/Debian
curl -fsSL https://deb.nodesource.com/setup_18.x | sudo -E bash -
sudo apt-get install -y nodejs

# CentOS/RHEL
curl -fsSL https://rpm.nodesource.com/setup_18.x | sudo bash -
sudo yum install -y nodejs

# 验证安装
node --version
npm --version
```

##### 2. 克隆项目

```bash
# 克隆仓库
git clone https://github.com/your-username/bucaoxin-website.git
cd bucaoxin-website

# 或上传项目文件到服务器
```

##### 3. 安装依赖

```bash
npm install
```

##### 4. 配置环境变量

```bash
# 创建环境变量文件
echo "NEXT_PUBLIC_SITE_URL=https://your-domain.com" > .env.production
```

##### 5. 构建项目

```bash
npm run build
```

##### 6. 使用 PM2 运行（推荐）

```bash
# 安装 PM2
npm install -g pm2

# 启动应用
pm2 start npm --name "bucaoxin-website" -- start

# 设置开机自启
pm2 startup
pm2 save

# 查看状态
pm2 status

# 查看日志
pm2 logs bucaoxin-website
```

##### 7. 配置 Nginx 反向代理

创建 Nginx 配置文件 `/etc/nginx/sites-available/bucaoxin-website`：

```nginx
server {
    listen 80;
    server_name your-domain.com www.your-domain.com;

    # 重定向到 HTTPS
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name your-domain.com www.your-domain.com;

    # SSL 证书配置（使用 Let's Encrypt）
    ssl_certificate /etc/letsencrypt/live/your-domain.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/your-domain.com/privkey.pem;

    # SSL 配置
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;
    ssl_prefer_server_ciphers on;

    # 代理到 Next.js
    location / {
        proxy_pass http://localhost:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # 静态资源缓存
    location /_next/static {
        proxy_pass http://localhost:3000;
        proxy_cache_valid 200 60m;
        add_header Cache-Control "public, immutable";
    }

    # Gzip 压缩
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_types text/plain text/css text/xml text/javascript application/x-javascript application/xml+rss application/javascript application/json;
}
```

启用配置：

```bash
# 创建符号链接
sudo ln -s /etc/nginx/sites-available/bucaoxin-website /etc/nginx/sites-enabled/

# 测试配置
sudo nginx -t

# 重载 Nginx
sudo systemctl reload nginx
```

##### 8. 配置 SSL 证书（Let's Encrypt）

```bash
# 安装 Certbot
sudo apt-get install certbot python3-certbot-nginx

# 获取证书
sudo certbot --nginx -d your-domain.com -d www.your-domain.com

# 自动续期（已自动配置）
sudo certbot renew --dry-run
```

##### 9. 配置防火墙

```bash
# Ubuntu/Debian (UFW)
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw enable

# CentOS/RHEL (firewalld)
sudo firewall-cmd --permanent --add-service=ssh
sudo firewall-cmd --permanent --add-service=http
sudo firewall-cmd --permanent --add-service=https
sudo firewall-cmd --reload
```

#### 使用 systemd（替代 PM2）

创建服务文件 `/etc/systemd/system/bucaoxin-website.service`：

```ini
[Unit]
Description=Bucaoxin Website
After=network.target

[Service]
Type=simple
User=www-data
WorkingDirectory=/var/www/bucaoxin-website
Environment=NODE_ENV=production
Environment=NEXT_PUBLIC_SITE_URL=https://your-domain.com
ExecStart=/usr/bin/npm start
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

启用服务：

```bash
# 重载 systemd
sudo systemctl daemon-reload

# 启动服务
sudo systemctl start bucaoxin-website

# 设置开机自启
sudo systemctl enable bucaoxin-website

# 查看状态
sudo systemctl status bucaoxin-website
```

---

### Docker 部署

#### 创建 Dockerfile

在项目根目录创建 `Dockerfile`：

```dockerfile
# 使用官方 Node.js 运行时作为基础镜像
FROM node:18-alpine AS base

# 安装依赖阶段
FROM base AS deps
WORKDIR /app

# 复制 package 文件
COPY package.json package-lock.json* ./
RUN npm ci

# 构建阶段
FROM base AS builder
WORKDIR /app
COPY --from=deps /app/node_modules ./node_modules
COPY . .

# 设置环境变量
ENV NEXT_TELEMETRY_DISABLED 1
ENV NODE_ENV production

# 构建应用
RUN npm run build

# 生产镜像
FROM base AS runner
WORKDIR /app

ENV NODE_ENV production
ENV NEXT_TELEMETRY_DISABLED 1

RUN addgroup --system --gid 1001 nodejs
RUN adduser --system --uid 1001 nextjs

# 复制构建产物
COPY --from=builder /app/public ./public
COPY --from=builder --chown=nextjs:nodejs /app/.next/standalone ./
COPY --from=builder --chown=nextjs:nodejs /app/.next/static ./.next/static

USER nextjs

EXPOSE 3000

ENV PORT 3000
ENV HOSTNAME "0.0.0.0"

CMD ["node", "server.js"]
```

#### 配置 Next.js 输出

在 `next.config.mjs` 中添加 standalone 输出：

```javascript
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'standalone', // 添加这一行
  // ... 其他配置
};

export default nextConfig;
```

#### 创建 .dockerignore

```
node_modules
.next
.git
.env*.local
*.log
.DS_Store
```

#### 构建和运行

```bash
# 构建镜像
docker build -t bucaoxin-website .

# 运行容器
docker run -p 3000:3000 \
  -e NEXT_PUBLIC_SITE_URL=https://your-domain.com \
  bucaoxin-website
```

#### 使用 Docker Compose

创建 `docker-compose.yml`：

```yaml
version: '3.8'

services:
  website:
    build: .
    ports:
      - "3000:3000"
    environment:
      - NEXT_PUBLIC_SITE_URL=https://your-domain.com
      - NODE_ENV=production
    restart: unless-stopped
    volumes:
      - ./public:/app/public
```

运行：

```bash
docker-compose up -d
```

---

### 其他平台

#### Netlify

1. 连接到 GitHub 仓库
2. 构建命令：`npm run build`
3. 发布目录：`.next`
4. 添加环境变量：`NEXT_PUBLIC_SITE_URL`

#### Railway

1. 连接 GitHub 仓库
2. 自动检测 Next.js 项目
3. 添加环境变量
4. 自动部署

#### Render

1. 创建新的 Web Service
2. 连接 GitHub 仓库
3. 构建命令：`npm run build`
4. 启动命令：`npm start`
5. 添加环境变量

#### Cloudflare Pages（支持 Next.js，国内访问相对较好）

Cloudflare Pages 是一个全栈平台，支持 Next.js 部署，并且在中国大陆的访问情况相对 Vercel 更好。

⚠️ **重要提示 - 中国大陆访问情况**：
- Cloudflare 在中国大陆有网络节点（与京东云合作）
- 访问速度相对 Vercel 更好，但仍可能受网络环境影响
- 使用自定义域名可以改善访问体验
- **如果主要面向中国用户，仍建议使用国内云服务商**

💡 **Cloudflare Pages 优势**：
- ✅ 全球 CDN 加速
- ✅ 免费 HTTPS 证书
- ✅ 自动部署（Git 推送触发）
- ✅ 预览部署（每个 PR 自动生成预览链接）
- ✅ 在中国大陆有网络节点
- ✅ 免费额度充足

**部署方式一：使用 Cloudflare Pages 适配器（推荐）**

这种方式支持完整的 Next.js 功能，包括 Server Components、API Routes 等。

1. **安装适配器**
   ```bash
   npm install --save-dev @cloudflare/next-on-pages
   ```

2. **配置 Next.js**
   
   **方式 A：使用 ES 模块（.mjs 文件）**
   在 `next.config.mjs` 中修改：
   ```javascript
   /** @type {import('next').NextConfig} */
   const nextConfig = {
     // ... 现有配置（图片、压缩等）
     images: {
       // 注意：静态导出时，images 配置可能需要调整
       unoptimized: process.env.CF_PAGES ? true : false,
       // ... 其他配置
     },
   };
   
   // 仅在 Cloudflare Pages 构建时使用适配器
   if (process.env.CF_PAGES) {
     const { withCloudflarePagesAdapter } = await import('@cloudflare/next-on-pages/next-config');
     export default withCloudflarePagesAdapter(nextConfig);
   } else {
     export default nextConfig;
   }
   ```
   
   **方式 B：使用 CommonJS（.js 文件，推荐）**
   将 `next.config.mjs` 重命名为 `next.config.js`，然后修改：
   ```javascript
   /** @type {import('next').NextConfig} */
   const nextConfig = {
     // ... 现有配置
   };
   
   // 仅在 Cloudflare Pages 构建时使用适配器
   if (process.env.CF_PAGES) {
     const { withCloudflarePagesAdapter } = require('@cloudflare/next-on-pages/next-config');
     module.exports = withCloudflarePagesAdapter(nextConfig);
   } else {
     module.exports = nextConfig;
   }
   ```
   
   **推荐**：对于本项目，由于主要是静态内容，建议使用**方式二（静态导出）**，无需配置适配器。

3. **更新 package.json**
   添加构建脚本：
   ```json
   {
     "scripts": {
       "build": "next build",
       "pages:build": "npx @cloudflare/next-on-pages"
     }
   }
   ```

4. **在 Cloudflare Pages 部署**
   - 登录 [Cloudflare Dashboard](https://dash.cloudflare.com/)
   - 进入 "Workers & Pages" → "Pages"
   - 点击 "Create a project" → "Connect to Git"
   - 选择 GitHub 仓库
   - 配置构建设置：
     - **Framework preset**: Next.js（自动检测）
     - **Build command**: `npm run build && npm run pages:build`
     - **Build output directory**: `.vercel/output/static`（适配器输出目录）
     - **Root directory**: `./`（默认）
     - **Node.js version**: 18 或更高
   - 添加环境变量：
     - `NEXT_PUBLIC_SITE_URL`: `https://your-domain.com`
     - `CF_PAGES`: `1`（标识 Cloudflare Pages 构建）
   - 点击 "Save and Deploy"

5. **自定义域名**
   - 在项目设置中点击 "Custom domains"
   - 添加自定义域名
   - 配置 DNS 记录（Cloudflare 会自动配置）
   - 等待 SSL 证书自动配置

**部署方式二：静态导出（适合纯静态网站）**

如果你的项目主要是静态内容（如本项目），可以使用静态导出。

1. **配置 Next.js 静态导出**
   在 `next.config.mjs` 中修改：
   ```javascript
   /** @type {import('next').NextConfig} */
   const nextConfig = {
     output: 'export', // 静态导出
     images: {
       // 静态导出需要禁用图片优化
       unoptimized: true,
       // ... 其他配置可以保留
       formats: ['image/avif', 'image/webp'],
     },
     // ... 其他配置保持不变
   };
   
   export default nextConfig;
   ```
   
   **注意**：
   - 静态导出会禁用所有服务器端功能（Server Components、API Routes 等）
   - 图片优化会被禁用，图片将直接使用原始格式
   - 如果项目中有使用 `getServerSideProps` 或 API Routes，不能使用静态导出

2. **在 Cloudflare Pages 部署**
   - 登录 Cloudflare Dashboard
   - 进入 "Workers & Pages" → "Pages"
   - 点击 "Create a project" → "Connect to Git"
   - 选择 GitHub 仓库
   - 配置构建设置：
     - **Framework preset**: Next.js（自动检测）
     - **Build command**: `npm run build`
     - **Build output directory**: `out`（静态导出目录）
     - **Root directory**: `./`（默认）
     - **Node.js version**: 18 或更高
   - 添加环境变量：
     - `NEXT_PUBLIC_SITE_URL`: `https://your-domain.com`
   - 点击 "Save and Deploy"

3. **注意事项**
   - 静态导出不支持 Server Components、API Routes 等服务器端功能
   - 如果使用 `getServerSideProps` 或 API Routes，需要使用方式一
   - 本项目主要是静态内容，可以使用静态导出

**推荐配置**：
- 对于本项目（主要是静态内容），推荐使用**方式二（静态导出）**
- 如果需要服务器端功能，使用**方式一（适配器）**

**环境变量配置**：
在 Cloudflare Pages 项目设置中：
1. 进入 "Settings" → "Environment variables"
2. 添加变量：
   - `NEXT_PUBLIC_SITE_URL`: `https://your-domain.com`
   - `CF_PAGES`: `1`（仅在使用适配器时需要）
3. 选择环境（Production, Preview, Browser preview）
4. 保存后重新部署

**通过 Cloudflare Wrangler CLI 部署**：
```bash
# 安装 Wrangler CLI
npm install -g wrangler

# 登录 Cloudflare
wrangler login

# 部署到 Cloudflare Pages
wrangler pages deploy .vercel/output/static --project-name=your-project-name
```

---

#### 阿里云/腾讯云/华为云（推荐国内用户使用）

**为什么选择国内云服务商？**
- ✅ 国内访问速度快
- ✅ 无需翻墙即可访问
- ✅ 支持备案
- ✅ 提供国内 CDN 加速
- ✅ 技术支持更及时

**部署步骤**：

1. **购买云服务器（ECS）**
   - 选择适合的配置（建议至少 2核2G）
   - 选择 Linux 系统（Ubuntu 20.04+ 或 CentOS 7+）
   - 配置安全组（开放 80、443 端口）

2. **按照"自己的服务器"部署步骤**
   - 安装 Node.js
   - 克隆项目
   - 安装依赖
   - 构建项目
   - 使用 PM2 运行

3. **配置云 CDN 加速**
   - 在云服务商控制台开通 CDN 服务
   - 添加域名并配置源站
   - 配置 HTTPS 证书
   - 配置缓存规则

4. **域名备案**（如使用国内服务器）
   - 在云服务商提交备案申请
   - 准备相关材料
   - 等待审核（通常 1-2 周）

5. **配置 DNS 解析**
   - 在域名服务商配置 A 记录或 CNAME
   - 指向服务器 IP 或 CDN 地址

**推荐配置**：
- **阿里云**：ECS + OSS + CDN + SSL 证书
- **腾讯云**：CVM + COS + CDN + SSL 证书
- **华为云**：ECS + OBS + CDN + SSL 证书

---

## ✅ 部署后验证

### 1. 功能测试

- [ ] 首页正常加载
- [ ] 所有页面可以访问
- [ ] 导航菜单正常工作
- [ ] 主题切换功能正常
- [ ] 表单提交功能正常（如已配置）
- [ ] 移动端响应式正常
- [ ] 图片正常加载
- [ ] 动画效果正常

### 2. SEO 验证

- [ ] 访问 `/sitemap.xml` 查看站点地图
- [ ] 访问 `/robots.txt` 查看爬虫规则
- [ ] 使用 Google Search Console 验证
- [ ] 使用以下工具测试 OpenGraph：
  - https://www.opengraph.xyz/
  - https://developers.facebook.com/tools/debug/
  - https://cards-dev.twitter.com/validator

### 3. 性能测试

使用以下工具测试性能：

- [Google PageSpeed Insights](https://pagespeed.web.dev/)
- [GTmetrix](https://gtmetrix.com/)
- [WebPageTest](https://www.webpagetest.org/)

### 4. 安全检查

- [ ] HTTPS 证书正常
- [ ] 安全头配置正确
- [ ] 没有控制台错误
- [ ] 没有安全漏洞

---

## ⚡ 性能优化

### 1. 启用 CDN

使用 CDN 加速静态资源：

- Cloudflare
- 阿里云 CDN
- 腾讯云 CDN

### 2. 图片优化

- 使用 Next.js Image 组件
- 启用 WebP/AVIF 格式
- 使用适当的图片尺寸
- 启用懒加载

### 3. 缓存策略

项目已配置以下缓存：

- 静态资源：长期缓存（1年）
- HTML：短期缓存（1小时）
- API：根据需求配置

### 4. 压缩

- 启用 Gzip/Brotli 压缩
- 代码压缩（已配置）
- 图片压缩（已配置）

---

## ❓ 常见问题

### 0. Vercel 在中国无法访问怎么办？

**问题**: Vercel 部署的网站在中国大陆无法访问或访问速度很慢

**原因**:
- Vercel 的服务器主要部署在海外
- 中国网络环境对海外服务有限制
- `.vercel.app` 域名可能被限制访问

**解决方案**:

1. **使用自定义域名 + 国内 CDN**（推荐）
   ```bash
   # 在 Vercel 添加自定义域名
   # 在阿里云/腾讯云配置 CDN
   # 将 CDN 回源地址设置为 Vercel 的域名
   ```

2. **使用国内云服务商部署**（最稳定）
   - 使用阿里云、腾讯云、华为云等国内服务商
   - 按照"自己的服务器"或"Docker 部署"步骤
   - 配置国内 CDN 加速

3. **使用国内反向代理**
   - 在国内服务器上配置 Nginx 反向代理
   - 代理到 Vercel 部署的网站
   - 使用国内 CDN 加速

4. **使用其他部署平台**
   - Cloudflare Pages（在中国大陆访问相对较好，推荐）
   - Netlify（同样可能受限）
   - Railway（可能受限）
   - Render（可能受限）
   - 国内平台：阿里云 Serverless、腾讯云 Serverless

**建议**:
- 如果主要用户在中国大陆，**强烈建议使用国内云服务商**
- 如果用户遍布全球，可以考虑 Vercel + 国内 CDN 的组合方案
- 如果预算充足，可以考虑使用多区域部署（国内 + 海外）

### 1. 构建失败

**问题**: `npm run build` 失败

**解决方案**:
```bash
# 清理缓存
rm -rf .next node_modules package-lock.json

# 重新安装依赖
npm install

# 重新构建
npm run build
```

### 2. 环境变量不生效

**问题**: 环境变量在构建后不生效

**解决方案**:
- 确保变量名以 `NEXT_PUBLIC_` 开头（客户端变量）
- 重新构建项目
- 检查环境变量是否正确配置

### 3. 图片不显示

**问题**: 图片加载失败

**解决方案**:
- 检查图片路径是否正确
- 确保图片文件在 `public` 目录
- 检查 Next.js 图片配置
- 检查 CDN 配置（如使用）

### 4. 样式不生效

**问题**: Tailwind CSS 样式不显示

**解决方案**:
```bash
# 清理缓存
rm -rf .next

# 重新构建
npm run build
```

### 5. 端口被占用

**问题**: 端口 3000 已被占用

**解决方案**:
```bash
# 修改端口
PORT=3001 npm start

# 或在环境变量中设置
export PORT=3001
```

### 6. PM2 进程崩溃

**问题**: PM2 进程频繁重启

**解决方案**:
```bash
# 查看日志
pm2 logs bucaoxin-website

# 检查内存使用
pm2 monit

# 增加内存限制（如需要）
pm2 start npm --name "bucaoxin-website" -- start --max-memory-restart 1G
```

### 7. SSL 证书问题

**问题**: SSL 证书错误或过期

**解决方案**:
```bash
# 更新 Let's Encrypt 证书
sudo certbot renew

# 重启 Nginx
sudo systemctl restart nginx
```

---

## 📊 监控和维护

### 1. 日志监控

#### PM2 日志

```bash
# 查看实时日志
pm2 logs bucaoxin-website

# 查看错误日志
pm2 logs bucaoxin-website --err

# 清理日志
pm2 flush
```

#### systemd 日志

```bash
# 查看服务日志
sudo journalctl -u bucaoxin-website -f

# 查看错误日志
sudo journalctl -u bucaoxin-website -p err
```

### 2. 性能监控

#### 使用 Vercel Analytics

如果使用 Vercel，可以启用 Analytics：

1. 在 Vercel Dashboard 启用 Analytics
2. 查看性能指标
3. 分析用户行为

#### 使用第三方监控

- [Sentry](https://sentry.io/) - 错误监控
- [LogRocket](https://logrocket.com/) - 会话重放
- [Google Analytics](https://analytics.google.com/) - 网站分析

### 3. 定期维护

#### 更新依赖

```bash
# 检查过时的依赖
npm outdated

# 更新依赖
npm update

# 更新 Next.js
npm install next@latest
```

#### 备份

定期备份：

- 代码仓库（Git）
- 数据库（如有）
- 配置文件
- 环境变量

#### 安全更新

- 定期更新系统
- 更新 Node.js 版本
- 更新依赖包
- 检查安全漏洞：`npm audit`

### 4. 监控指标

关注以下指标：

- 响应时间
- 错误率
- 内存使用
- CPU 使用
- 磁盘空间
- 带宽使用

---

## 📝 部署清单

部署前检查：

- [ ] 代码已提交到 Git 仓库
- [ ] 环境变量已配置
- [ ] 本地构建测试通过
- [ ] 图片资源已准备
- [ ] 域名已配置
- [ ] SSL 证书已配置（如需要）
- [ ] DNS 记录已配置
- [ ] 防火墙规则已配置
- [ ] 监控已设置
- [ ] 备份策略已制定

---

## 🔗 相关链接

- [Next.js 部署文档](https://nextjs.org/docs/deployment)
- [Vercel 文档](https://vercel.com/docs)
- [PM2 文档](https://pm2.keymetrics.io/docs/)
- [Nginx 文档](https://nginx.org/en/docs/)
- [Docker 文档](https://docs.docker.com/)

---

## 💡 最佳实践

1. **使用 CI/CD**: 自动化部署流程
2. **环境分离**: 开发、测试、生产环境分离
3. **版本控制**: 使用 Git 管理代码
4. **监控告警**: 设置监控和告警
5. **定期备份**: 定期备份重要数据
6. **安全更新**: 及时更新安全补丁
7. **性能优化**: 持续优化性能
8. **文档维护**: 保持文档更新

---

## 🆘 获取帮助

如果遇到问题：

1. 查看本文档的常见问题部分
2. 检查 Next.js 官方文档
3. 查看项目 Issues
4. 联系技术支持

---

**最后更新**: 2024年
**文档版本**: 1.0.0

