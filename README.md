# 布草芯科技官网

这是使用 Next.js 14、TypeScript 和 Tailwind CSS 构建的现代化企业官网项目。

## ✨ 功能特性

- 🎨 现代化设计，支持亮色/暗色主题切换
- 📱 完全响应式，适配所有设备
- ⚡ 性能优化，快速加载
- 🎯 SEO 友好
- 🔄 平滑的页面切换和动画效果
- 📊 丰富的内容展示（服务、案例、新闻、关于）
- 📮 在线联系表单
- 🔝 返回顶部按钮

## 🛠 技术栈

- **框架**: Next.js 14 (App Router)
- **语言**: TypeScript
- **样式**: Tailwind CSS
- **图标**: Lucide React
- **工具**: class-variance-authority, clsx, tailwind-merge

## 🚀 快速开始

### 方式一：使用批处理文件（Windows）

双击运行：
- `install.bat` - 安装依赖
- `start.bat` - 启动开发服务器

### 方式二：使用命令行

```bash
# 安装依赖
npm install

# 运行开发服务器
npm run dev

# 构建生产版本
npm run build

# 启动生产服务器
npm run start
```

打开浏览器访问 [http://localhost:3000](http://localhost:3000)

## 📁 项目结构

```
├── app/                    # Next.js App Router 页面
│   ├── page.tsx           # 首页
│   ├── services/          # 服务页面
│   ├── about/             # 关于页面
│   ├── contact/           # 联系页面
│   ├── news/              # 新闻页面
│   ├── portfolio/         # 案例页面
│   ├── layout.tsx         # 根布局
│   └── globals.css        # 全局样式
├── components/            # React 组件
│   ├── Header.tsx        # 导航栏（支持主题切换）
│   ├── Footer.tsx        # 底部
│   ├── Hero.tsx          # 首页 Hero 区域
│   ├── Services.tsx      # 服务组件
│   ├── Portfolio.tsx     # 案例组件
│   └── BackToTop.tsx     # 返回顶部按钮
├── lib/                   # 工具函数
│   └── utils.ts          # 通用工具
└── public/               # 静态资源
```

## 🎨 主题说明

项目支持亮色/暗色两种主题：

- **亮色模式**: 浅黄色背景（`yellow-50`），温暖舒适
- **暗色模式**: 深色渐变背景，现代专业
- 主题选择会自动保存到浏览器本地存储

## 📦 部署

详细部署说明请查看 [部署文档.md](./部署文档.md)

### 快速部署：

**Vercel（推荐，最简单）**:
1. 推送代码到 GitHub
2. 在 [Vercel](https://vercel.com) 导入项目
3. 配置环境变量 `NEXT_PUBLIC_SITE_URL`
4. 自动部署完成

**Docker**:
```bash
docker build -t bucaoxin-website .
docker run -p 3000:3000 -e NEXT_PUBLIC_SITE_URL=https://your-domain.com bucaoxin-website
```

**自己的服务器**:
```bash
npm run build
npm run start
# 使用 PM2 保持运行
pm2 start npm --name "bucaoxin-website" -- start
```

更多部署方式请查看 [部署文档.md](./部署文档.md)

## 🔧 自定义配置

### 修改公司信息

在以下文件中修改：
- `components/Header.tsx` - Logo 和公司名称
- `components/Footer.tsx` - 联系方式
- `app/contact/page.tsx` - 详细联系信息

### 修改主题色

在 `tailwind.config.ts` 中自定义颜色：
```typescript
theme: {
  extend: {
    colors: {
      // 自定义颜色
    }
  }
}
```

## 📝 开发说明

- 使用 TypeScript 进行类型检查
- 遵循 ESLint 代码规范
- 使用 Tailwind CSS 原子化 CSS
- 组件采用函数式组件 + Hooks
- 支持服务端渲染（SSR）和静态生成（SSG）

## 🐛 常见问题

### 1. 端口被占用
```bash
# 修改端口
PORT=3001 npm run dev
```

### 2. 样式不生效
```bash
# 清理缓存
rm -rf .next
npm run dev
```

### 3. 构建失败
```bash
# 重新安装依赖
rm -rf node_modules package-lock.json
npm install
```

## 📄 License

MIT

## 🤝 贡献

欢迎提交 Issue 和 Pull Request！

---

**Made with ❤️ by 布草芯科技**
# bcxtech
